import 'package:flutter/material.dart';

class UserCancelledAppointmentsTab extends StatelessWidget {
  const UserCancelledAppointmentsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("No cancelled appointments."),
    );
  }
}
