import 'package:flutter/material.dart';

class UserPastAppointmentsTab extends StatelessWidget {
  const UserPastAppointmentsTab({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("You have no past appointments."),
    );
  }
}
